/* eslint-disable prettier/prettier */
const initialState = {
  role:null,
  token:null,
  email:null,
  password:null
};

const reducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default reducer;
